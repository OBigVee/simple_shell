.\" Automatically generated by Pandoc 2.5
.\"
.TH "man SIMPLE_SHELL" "" "" "" ""
.hy
.SH Name
.PP
\f[B]simple_shell\f[R] \- is a simple UNIX command interpreter
.SH SYNOPSIS
.PP
\f[B]int hsh (void)\f[R]
.SH DESCRIPTION
.PP
\f[B]Overview\f[R]
.PP
This simple shell is a program tha will read lines from either a file or
the interprets them and then execute them.
Users can therefore interactively type shell directly or pipe shell
scripts.
.PP
\f[B]Invocation\f[R] if there are no args present and std input of the
shell is connected to terminal, it implies the shell is interactive.
an interactive shell enables you to type command and get back output
from those commands.
.PP
\f[B]Builtins\f[R] The builtin commands to perform some ops are listed
below.
.IP \[bu] 2
\[lq]\-exit [status]\[rq] command terminates the shell process if not
the exit status of preceeding command is used.
.IP \[bu] 2
\[lq]\-help[builtin]\[rq] prints a usage message
.IP \[bu] 2
\[lq]\-unsetenv [variable]\[rq] unsets an environmental variable with
name [variable]
.IP \[bu] 2
\[lq]\-setenv [key] [value]\[rq] Sets a local environment variable with
key value pair.
if key already exits, this action will replace the existing key with new
value.
.IP \[bu] 2
\[lq]\-cd [directory]\[rq] Change directory to a targeted directory and
refer to some basics shell
navigation (http://linuxcommand.org/lc3_lts0020.php)
.IP \[bu] 2
\[lq]\-alias\[rq] alias command always work in the current version.
.IP \[bu] 2
\[lq]\-history\[rq] works in the current version.
.SH EXIT STATUS
.PP
shell will exit with a non\-zero exits status when errors such as syntax
error if not, shell returns the last command executed or exit with a
return arg if exit builtin is used.
.SH ENVIRONMENT
.IP \[bu] 2
HOME The user\[cq]s login directory
.IP \[bu] 2
PATH The defualt search path for executables
.IP \[bu] 2
PS1 The primary prompt string, which defaults to \[lq]$\[rq]
.SH FILES
.SH BUGS
.IP \[bu] 2
expansion works once by reading per standard input.
.SH AUTHOR
.PP
Victor Adedeji \&...
